Augedit Installation
------------------

Augedit depends on:

  * glib
  * gtk+
  * augeas-vala

To build and install Augedit, type the following commands:

$ cmake -DCMAKE_INSTALL_PREFIX:PATH=/usr .
$ make
$ sudo make install

The make target "test" runs unit tests.
